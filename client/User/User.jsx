import React, { Component } from 'react';

function User(props){
  const [id, setID] = useState(null);

    return (
      <div>
        <p>This is the user page</p>
      </div>
    );
}

export default User;